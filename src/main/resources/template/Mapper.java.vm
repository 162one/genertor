package ${package}.${moduleName}.mapper;


import ${package}.${moduleName}.domain.${className};
import org.apache.ibatis.annotations.Param;

import java.io.Serializable;
import java.util.Collection;

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper {

    /**
     * ${comments}列表信息
     *
     * @return 结果
     */
    PageBean<${className}> page();

    /**
     * ${comments}详细信息
     *
     * @param ${pk.attrname} ${comments}ID
     * @return 结果
     */
    ${className} selectByPrimaryKey(@Param("${pk.attrname}")Long ${pk.attrname});

    /**
     * 保存${comments}信息
     *
     * @param ${pk.attrname}
     * @return 结果
     */
    void insertSelective(${className} ${classname});

    /**
     * 修改${comments}信息
     *
     * @param ${pk.attrname}
     * @return 结果
     */
    void updateByPrimaryKeySelective(${className} ${classname});

    /**
     * 批量删除 ${comments}
     *
     * @param idList 需要删除的 ${comments}ID
     * @return 结果
     */
    Integer deleteByIds(@Param("idList") Collection<? extends Serializable> idList);
}
